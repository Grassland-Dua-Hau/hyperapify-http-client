{
  "name": "hyperapify-http-client",
  "version": "0.1.0",
  "packageManager": "npm@10.8.2",
  "description": "A Node package library in TypeScript creates an HTTP client that accesses the Hyperapify API, authenticated using hmac-auth, which is automatically generated by @hey-api/openapi-ts.",
  "keywords": [
    "typescript",
    "boilerplate",
    "template",
    "starter",
    "scaffolding",
    "cli",
    "claude-code",
    "anthropic",
    "ai-development",
    "llm-development",
    "skills",
    "sub-agents",
    "agentic",
    "workflow",
    "multi-agent"
  ],
  "author": "Shinsuke Kagawa",
  "license": "MIT",
  "type": "module",
  "repository": {
    "type": "git",
    "url": "https://github.com/shinpr/ai-coding-project-boilerplate.git"
  },
  "scripts": {
    "openapi-ts": "openapi-ts && cp tsconfig.api.json src/api/tsconfig.json && node scripts/generate-url-map.js && biome format --write src/services/url-map.ts",
    "build": "tsc && tsc-alias",
    "build:backend": "tsc && tsc-alias",
    "dev": "tsx src/index.ts",
    "watch": "tsx watch src/index.ts",
    "type-check": "tsc --noEmit",
    "test": "vitest run",
    "test:ui": "vitest --ui",
    "test:coverage": "vitest run --coverage",
    "test:coverage:summary": "node scripts/show-coverage.js",
    "test:coverage:clean": "rm -rf coverage .vitest-cache",
    "test:coverage:fresh": "npm run test:coverage:clean && npm run test:coverage",
    "test:watch": "vitest",
    "format": "biome format --write src",
    "format:check": "biome format src",
    "lint": "biome lint src",
    "lint:fix": "biome lint --write src",
    "check": "biome check src",
    "check:fix": "biome check --write src",
    "check:unused": "knip --include exports",
    "check:deps": "madge --circular --extensions ts src",
    "check:deps:graph": "madge --extensions ts --image graph.svg src",
    "check:code": "npm run check && npm run check:unused && npm run check:deps && npm run build",
    "check:all": "npm run check:code && npm run test",
    "cleanup:processes": "bash ./scripts/cleanup-test-processes.sh",
    "test:safe": "npm test && npm run cleanup:processes",
    "lang:ja": "node scripts/set-language.js ja",
    "lang:en": "node scripts/set-language.js en",
    "prepare": "husky"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.4.6",
    "@hey-api/openapi-ts": "^0.94.2",
    "@types/node": "^22.0.0",
    "@vitest/coverage-v8": "^4.0.0",
    "@vitest/ui": "^4.0.0",
    "fast-check": "^3.23.2",
    "husky": "^9.1.7",
    "knip": "^5.0.0",
    "lint-staged": "^16.1.0",
    "madge": "^8.0.0",
    "tsc-alias": "^1.8.7",
    "tsx": "^4.19.4",
    "typescript": "^5.0.0",
    "vite": "^6.0.0",
    "vitest": "^4.0.0"
  },
  "engines": {
    "node": ">=20"
  },
  "lint-staged": {
    "src/**/*.{ts,tsx}": [
      "biome check --write --no-errors-on-unmatched",
      "biome format --write --no-errors-on-unmatched"
    ]
  }
}
